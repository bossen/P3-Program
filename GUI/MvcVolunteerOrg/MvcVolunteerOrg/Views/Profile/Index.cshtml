@model IEnumerable<Model.User>

@* THIS IS FOR TESTING *@



@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Profile</legend>

        @Html.DisplayNameFor(model => model.Name)
        @Html.DisplayNameFor(model => model.Email)

        @*@foreach(var item in Model){
            
        @Html.DisplayFor(modelItem => item.Username)
        @Html.DisplayFor(modelItem => item.Email)

        }
        
        This is a problem*@

    </fieldset>
}

<input id="Button1" type="button" value="button" name="Test" onclick="javascript: window.open('/Profile/Edit',
                                       'Customer Search',
                                       'height=' + (window.screen.height - 100) + ',width=200,left=' + (window.screen.width - 250) + ',top=10,status=no,toolbar=no,resizable=yes,scrollbars=yes');"/>